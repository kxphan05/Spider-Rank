# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Large/generated data artifacts
data/catalog.jsonl
data/catalog.jsonl.gz
results.json
*.pyc
data/dense_index/
data/user_profiles.json
model/

# Local tool state, not project source. The skills directory is the exception:
# CLAUDE.md cites .claude/skills/retrieval-experiments/SKILL.md as the
# authoritative write-up for three closed investigations, so leaving it
# untracked made those citations dangle on a fresh clone.
.claude/*
!.claude/skills/
logs/

# Local experiment logs
*.log
